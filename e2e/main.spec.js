'use strict';

describe('The Login page', function () {
  var page;

  beforeEach(function () {
    browser.get('http://localhost:9000/index.html');
  });

 
  it('Login Page', function () {
    expect(true);
  });

});
